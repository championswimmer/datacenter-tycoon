{
  "name": "@datacenter-tycoon/web",
  "version": "0.1.0",
  "description": "Web frontend for Datacenter Tycoon.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'web build not yet configured' && exit 0",
    "dev": "echo 'web dev not yet configured' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx 'src/**/*.test.ts'",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@datacenter-tycoon/game-logic": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
