# Desktop build assets

Replace these placeholders with production-grade platform icons before release.

Recommended follow-up assets:
- `icon.icns` for macOS
- `icon.ico` for Windows
- `icon.png` for Linux and generic packaging metadata
